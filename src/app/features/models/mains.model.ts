export class Mains {
    category: string;
    name: string;
    prix: number;

    constructor(category: string, name: string, prix: number) {
        this.category = category;
        this.name = name;
        this.prix = prix;
    }
}
